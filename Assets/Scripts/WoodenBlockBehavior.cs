using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WoodenBlockBehavior : MonoBehaviour
{
    public bool IsTouch;
    // Start is called before the first frame update
    void Start()
    {
        IsTouch = false;
    }
}
